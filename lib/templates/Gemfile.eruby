source 'https://rubygems.org'

ruby '<%= RUBY_VERSION %>'

<% for gem in @obj.config['gems']['production'] %>
gem '<%= gem %>'
<% end %>

group :test do
<% for gem in @obj.config['gems']['test'] %>
  gem '<%= gem %>'
<% end %>
<% for gem in @obj.config['gems']['test_require_false'] %>
  gem '<%= gem %>', require: false
<% end %>
end
