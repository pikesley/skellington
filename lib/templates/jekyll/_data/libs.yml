js:
<% lines = @gen.config['bootstrap']['js'].to_yaml.split "\n" %>
<% lines.shift %>
<% lines.each do |l| %>
<%= '  %s' %l.gsub('"', '').gsub('url: ', 'url:  ') %>
<% end %>

css:
  - name: foo
    bar:  //bar.com/baz.css

fonts:
  - name: ubuntu
    url:  //fonts.googleapis.com/css?family=Ubuntu+Mono
